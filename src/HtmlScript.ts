import * as jsdom from 'jsdom';
import * as fs from 'fs/promises';

export default class HtmlScript {
    constructor(scripts: string){

    }

    public static async loadFromFile(path: string, encoding: string = 'utf-8') {

    }

}